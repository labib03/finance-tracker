import TransaksiView from '@/modules/dashboard/views/TransaksiView';

export const metadata = {
  title: 'Daftar Transaksi - Financer Pro',
};

export default function TransaksiPage() {
  return <TransaksiView />;
}
