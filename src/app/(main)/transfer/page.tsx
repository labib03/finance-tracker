import TransferView from '@/modules/dashboard/views/TransferView';

export const metadata = {
  title: 'Transfer Akun - Financer Pro',
};

export default function TransferPage() {
  return <TransferView />;
}
